and the forward paths (with associated Q(s)) linking
it to the input (transformed) variables Q/S, IRo, ILo,
C,(s), and C,(s). The expressions in the text only
contain one pole and are much simpler than the
general case because of the assumption that the
inputs are complementary (see main text, equafions
4 and 5).
